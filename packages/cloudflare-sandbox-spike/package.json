{
  "name": "@stackmemoryai/cloudflare-sandbox-spike",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "P3 spike for a Cloudflare-hosted StackMemory sandbox runtime.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typegen": "wrangler types",
    "check": "node --check src/index.js"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.41.0"
  }
}
