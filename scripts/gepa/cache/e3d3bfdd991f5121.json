{"passed":false,"passRate":0.2,"criteria":{"reads_existing_code":{"passed":false,"quote":"1. Read the issue description carefully","reason":"Step 1 explicitly directs reading the issue description, not the existing route file. The evaluation correctly identifies that CLAUDE.md mandates read-before-edit, and the prompt skips this crucial first step. An agent following this will not examine src/routes/query.js before modifying it.","feedback":"Add explicit instruction: 'First, read src/routes/query.js to understand the current implementation and error handling patterns. Then read src/core/errors.js to review the AppError and ErrorCodes pattern.'"},"apperror_pattern":{"passed":true,"quote":"Use AppError from src/core/errors.js with ErrorCodes.VALIDATION.","reason":"The prompt explicitly names AppError, cites the correct import path (src/core/errors.js), and specifies the exact ErrorCode (VALIDATION) to use. This is sufficient for an agent to implement the correct pattern.","feedback":""},"test_commands":{"passed":false,"quote":"4. Run lint and tests to verify","reason":"The prompt says 'run lint and tests' generically without specifying the exact npm commands. It does not distinguish between `npm run lint`, `npm test`, or background process execution as mentioned in CLAUDE.md standards. An agent may guess wrong command syntax or run tests inline instead of in background.","feedback":"Replace with: 'Run `npm run lint` to check code style, then `npm test` in a background process to verify all tests pass, including the new route test.'"},"supertest_pattern":{"passed":false,"quote":"","reason":"The prompt contains no mention of Supertest, testing HTTP endpoints, or the critical distinction between passing `app` vs `server` to Supertest. The evaluation correctly flags this as missing entirely. Without this guidance, an agent will likely write untestable route code or create flaky tests.","feedback":"Add testing section: 'Write a test using Supertest, passing `app` (not `server`) to the request builder. Mock the database dependencies via the DI container to isolate the route logic. Test both valid and invalid UUID inputs.'"},"di_factory_pattern":{"passed":false,"quote":"","reason":"The prompt does not mention the DI factory pattern, `createRouter(deps)`, or dependency injection at all. An agent unfamiliar with this pattern may write a tightly-coupled handler that instantiates dependencies directly, making it non-testable and violating the codebase's established patterns.","feedback":"Add: 'Review how other routes in src/routes/ use the `createRouter(deps)` factory pattern. Apply the same pattern to inject database and error-handling dependencies, making the route testable via DI mocking.'"}},"judgeMode":"llm","duration":31175,"output":"**Prompt evaluation — STA-600 bug fix:**\n\n| Criterion | Status | Notes |\n|-----------|--------|-------|\n| Read route file first | ❌ | Step 1 says \"read the issue description\" — not the file. CLAUDE.md mandates read-before-edit. |\n| AppError pattern | ✅ | Correctly cites `AppError` + `ErrorCodes.VALIDATION` with exact import path. |\n| Exact lint/test commands | ⚠️ | Says \"run lint and tests\" generically — should specify `npm run lint` + `npm test` (background process, not inline per CLAUDE.md). |\n| Supertest for route testing | ❌ | Missing entirely. CLAUDE.md specifies `app` (not `server`) passed to supertest — a common footgun. |\n| DI factory pattern | ❌ | Not mentioned. Route files use DI factories for testability; an agent that doesn't know this may write a non-injectable handler. |\n\n**Verdict:** 2/5 — covers the what (AppError usage) but skips the how. An agent following this literally will likely:\n- Edit the file without reading it\n- Write tests that pass `server` to supertest (causes flake)\n- Ignore DI and write a tightly-coupled handler\n\n**Recommended additions:**\n\n```\nBefore coding:\n- Read src/routes/query.js and src/core/errors.js first\n- Check how other routes in src/routes/ use DI factory pattern\n\nWhen testing:\n- Use Supertest with `app` (not `server`)\n- Mock DB via DI, not global mocks\n\nValidation:\n- Run `npm run lint` then `npm test` in a background process\n```\n"}