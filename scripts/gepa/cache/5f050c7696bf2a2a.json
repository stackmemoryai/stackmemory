{"passed":false,"passRate":0,"criteria":{"ctx_schema":{"passed":false,"quote":"`ctx_nodes` mentioned once (\"find ctx_nodes with conflicting content\") but no schema detail; `ctx_edges` absent entirely","reason":"The prompt names ctx_nodes in passing but provides zero schema documentation. ctx_edges is not mentioned at all. A developer would not know the column names, data types, or relationships needed to implement the function correctly.","feedback":"Add explicit schema reference: 'Schema: ctx_nodes(id, tenant_id, entity_id, content, embedding), ctx_edges(src_id, dst_id, rel_type, weight). Use these tables to find conflicting claims about the same entity.'"},"tenant_scoping":{"passed":false,"quote":"No mention of tenant isolation or multi-tenant constraints","reason":"The prompt contains zero instructions about tenant_id filtering. A developer following this would likely write queries that leak data across tenants, creating a critical security vulnerability.","feedback":"Add mandatory guidance: 'All queries must scope to tenant_id — never query ctx_nodes or ctx_edges without filtering by tenant_id. Ensure the getContradictions(tenant_id, entity_id) signature enforces this.'"},"existing_service":{"passed":false,"quote":"Names the file and mentions the stub exists, but never says 'read it first'","reason":"The prompt mentions 'src/ctx/graph-query.js' and that a stub exists, but provides no explicit instruction to read the existing service first. This is only 'implicit' — a weak signal that a developer might skip understanding the current codebase structure and patterns.","feedback":"Add explicit instruction: 'Read src/ctx/graph-query.js to understand the existing service structure, helper functions, and query patterns before implementing getContradictions().'"},"graph_service_wrapper":{"passed":false,"quote":"Not referenced at all","reason":"GraphService at src/graph/service.ts is completely absent from the prompt. Without this reference, a developer would likely bypass the abstraction layer and write raw queries, duplicating logic and violating architectural boundaries.","feedback":"Add clear directive: 'Use the GraphService wrapper (src/graph/service.ts) for all graph traversal — do not write raw database queries. Consult the service's public API for querying ctx_nodes and ctx_edges.'"}},"judgeMode":"llm","duration":34472,"output":"The repo has no source files — only `CLAUDE.md`. Evaluating the prompt template against the stated criteria purely on content:\n\n**Checklist:**\n\n| Convention | Present? | Notes |\n|---|---|---|\n| `ctx_nodes`/`ctx_edges` schema reference | Partial | `ctx_nodes` mentioned once (\"find ctx_nodes with conflicting content\") but no schema detail; `ctx_edges` absent entirely |\n| `tenant_id` scoping | No | No mention of tenant isolation or multi-tenant constraints |\n| Instructs reading existing `graph-query.js` | Implicit only | Names the file and mentions the stub exists, but never says \"read it first\" |\n| `GraphService` wrapper at `src/graph/service.ts` | No | Not referenced at all |\n\n**Summary:**\n\nThe prompt fails 3 of 4 conventions outright, and the 4th (read existing service) is only implied. A coding agent following this prompt would likely:\n- Implement contradiction logic without knowing the edge schema or traversal patterns\n- Skip tenant isolation, introducing a multi-tenant data leak risk\n- Ignore the `GraphService` abstraction and write raw DB queries or duplicate logic\n\n**Recommended additions:**\n\n1. Add: *\"Read `src/ctx/graph-query.js` and `src/graph/service.ts` before implementing.\"*\n2. Add: *\"All queries must scope to `tenant_id` — never query `ctx_nodes` or `ctx_edges` without a tenant filter.\"*\n3. Add: *\"Schema: `ctx_nodes(id, tenant_id, entity_id, content, embedding)`, `ctx_edges(src_id, dst_id, rel_type, weight)`.\"*\n4. Add: *\"Use `GraphService` (src/graph/service.ts) for graph traversal — do not write raw graph queries.\"*\n"}