{"passed":false,"passRate":0.25,"criteria":{"ctx_schema":{"passed":false,"quote":"Mentions `ctx_nodes` once but omits `ctx_edges` entirely — contradiction detection likely requires edge traversal","reason":"The prompt references 'ctx_nodes' in the task description ('find ctx_nodes with conflicting content') but does not mention 'ctx_edges' schema. For contradiction detection across entities, understanding the relationship structure (edges) is essential. The evaluation correctly identifies this as a partial failure since only one of the two required schema components is referenced.","feedback":"The prompt template should explicitly reference both `ctx_nodes` AND `ctx_edges` schemas. Add language like: 'Use the ctx_nodes and ctx_edges schema (see src/ctx/schema.js) to query for contradictory claims. ctx_nodes contain entity claims; ctx_edges define relationships between them.'"},"tenant_scoping":{"passed":false,"quote":"No mention of multi-tenant isolation; critical omission given the project's multi-tenant container model","reason":"The prompt template provided makes zero mention of `tenant_id` scoping. The evaluation notes this is a 'critical omission' for a multi-tenant system. Without explicit instruction, agents may write queries that inadvertently cross tenant boundaries or fail to filter by tenant_id, creating a serious data isolation vulnerability.","feedback":"Add mandatory tenant scoping language to the prompt: 'All queries must filter by tenant_id to prevent cross-tenant data leakage. Every ctx_nodes and ctx_edges query must include a WHERE clause scoping to the current tenant. Do not assume tenant context is implicit.'"},"existing_service":{"passed":true,"quote":"Explicitly names `src/ctx/graph-query.js` and the `getContradictions()` stub","reason":"The original prompt states 'The context graph query service (src/ctx/graph-query.js) has a getContradictions() stub. Implement it...' This directly instructs the agent to work with and understand the existing service. The evaluation confirms this criterion is met with a 'Yes' result.","feedback":""},"graph_service_wrapper":{"passed":false,"quote":"No mention of the GraphService wrapper; agents may bypass it and write directly against the DB","reason":"The prompt template does not reference `src/graph/service.ts` or any GraphService abstraction layer. The evaluation flags this as a missing convention that creates risk: agents may implement raw database queries instead of using the intended service layer, leading to inconsistent patterns and architectural violations.","feedback":"Add explicit instruction: 'Use the GraphService wrapper (src/graph/service.ts) for all graph operations. Do not query ctx_nodes or ctx_edges directly from the database. This ensures consistent caching, validation, and tenant isolation.'"}},"judgeMode":"llm","duration":38956,"output":"The repo only contains `CLAUDE.md` — no source files exist. I can't verify the actual schema, service files, or conventions from the codebase. Evaluating the prompt purely against the stated criteria:\n\n---\n\n**Evaluation: STA-650 prompt vs. ProvenantAI context graph conventions**\n\n| Check | Result | Notes |\n|---|---|---|\n| References `ctx_nodes`/`ctx_edges` schema | **Partial** | Mentions `ctx_nodes` once but omits `ctx_edges` entirely — contradiction detection likely requires edge traversal |\n| Mentions `tenant_id` scoping | **Missing** | No mention of multi-tenant isolation; critical omission given the project's multi-tenant container model |\n| Instructs reading existing `graph-query.js` | **Yes** | Explicitly names `src/ctx/graph-query.js` and the `getContradictions()` stub |\n| References `GraphService` at `src/graph/service.ts` | **Missing** | No mention of the GraphService wrapper; agents may bypass it and write directly against the DB |\n\n**Summary:** 2/4 conventions met. Key gaps:\n\n1. **tenant_id scoping** — the prompt will produce code that ignores tenant isolation unless the agent infers it from context. High risk for a context graph query.\n2. **GraphService wrapper** — omitting this reference likely causes agents to implement raw queries instead of using the abstraction layer, creating inconsistency.\n3. **ctx_edges schema** — contradiction detection across entities requires edge traversal; the prompt only mentions nodes.\n\n**Recommended additions to the prompt template:**\n```\n- Scope all queries by tenant_id (never cross-tenant reads)\n- Use the GraphService wrapper (src/graph/service.ts) — do not query ctx_nodes/ctx_edges directly\n- Contradiction detection requires checking ctx_edges for entity relationships, not just ctx_nodes in isolation\n- Read src/ctx/graph-query.js before implementing\n```\n"}