{"passed":false,"passRate":0,"criteria":{"read_existing_templates":{"passed":false,"quote":"References `028_expanded_recipe_templates.sql` as a pattern to follow, but doesn't explicitly instruct reading it before writing","reason":"The prompt says 'Follow the existing template pattern in src/db/migrations/028_expanded_recipe_templates.sql' but uses 'follow' as a directive rather than an explicit instruction to read and study the file first. A strict reading convention requires the agent to review existing code before implementing. The phrasing assumes the agent will infer this step, rather than stating it as a prerequisite.","feedback":"Rewrite instruction 1 to be explicit: 'Before implementing, read src/db/migrations/028_expanded_recipe_templates.sql to understand the template structure and naming conventions.' This removes ambiguity and ensures the agent prioritizes pattern review."},"step_runner_stages":{"passed":false,"quote":"No mention of pipeline stages (validate → enrich → transform → dispatch)","reason":"The prompt mentions 'enrichment (LLM summarize + Clearbit company lookup), output to Slack' but does not reference the step-runner pipeline model. The prompt describes *what steps to do* (enrich, output) but not *which pipeline stage each step belongs to*. ProvenantAI convention requires explicit stage declarations (validate/enrich/transform/dispatch) for recipe composition.","feedback":"Add guidance: 'Each recipe step must declare its pipeline stage. For this recipe: the webhook input is the validate stage, LLM+Clearbit lookups are enrich stages, and Slack output is the dispatch stage. Ensure each step includes the correct `stage` field in the migration.'"},"enrichment_cjs":{"passed":false,"quote":"No mention of `require()` vs `import`, CJS constraint absent","reason":"The prompt is silent on module system requirements. ProvenantAI enrichment services use CommonJS, not ESM. An agent unfamiliar with this constraint might use modern `import` syntax in the enrichment step definition or service registration, causing a runtime module resolution failure. The prompt should explicitly guard against this.","feedback":"Add to instructions: 'The enrichment service at src/services/enrichment/ uses CommonJS (require, not import). When referencing enrichment handlers in the recipe template, use CJS syntax. Do not use ESM import statements.'"},"recipe_service_path":{"passed":false,"quote":"`src/recipes/service.js` not referenced at all","reason":"The prompt instructs writing a SQL migration but omits the requirement to register the new template in the service layer. ProvenantAI recipes must be registered in src/recipes/service.js for lifecycle management (instantiation, validation, execution). Without this reference, the agent will create an orphaned migration that exists in the database but is not accessible to the runtime.","feedback":"Add instruction: 'After creating the SQL migration, register template #035 in src/recipes/service.js following the existing switch/case pattern in the templateFactory function. Ensure the service loads and validates the new template on startup.'"}},"judgeMode":"llm","duration":58920,"output":"Evaluating the prompt against the 5 criteria:\n\n| Check | Result | Notes |\n|---|---|---|\n| Reads existing migrations first | **Partial** | References `028_expanded_recipe_templates.sql` as a pattern to follow, but doesn't explicitly instruct reading it before writing |\n| Step-runner pipeline stages | **Missing** | No mention of pipeline stages (validate → enrich → transform → dispatch) |\n| Enrichment service CJS (not ESM) | **Missing** | No mention of `require()` vs `import`, CJS constraint absent |\n| Tier enforcement | **Missing** | No mention of tier checks (free/pro/enterprise gate on enrichment steps) |\n| Recipe service path | **Missing** | `src/recipes/service.js` not referenced at all |\n\n**Summary**: 1/5 criteria met (partially). The prompt tells the agent what to build and where to look for a pattern, but omits all execution constraints — the pipeline stage model, the CJS module requirement for the enrichment service, tier gating, and the service file the new template must integrate with.\n\n**Risk**: An agent following this prompt will likely write a syntactically correct SQL migration but produce a recipe that either fails at runtime (ESM import in CJS context), skips tier enforcement, or isn't wired into the service layer.\n\n**Recommended additions:**\n- \"Read `src/db/migrations/028_expanded_recipe_templates.sql` and `src/recipes/service.js` before writing anything\"\n- \"The enrichment service uses CommonJS (`require`), not ESM — do not use `import`\"\n- \"Add tier enforcement: Clearbit enrichment is a pro-tier step — include `tier: 'pro'` in the step definition\"\n- \"Register the new template in `src/recipes/service.js` following the existing switch/case pattern\"\n- \"Pipeline stages are: `validate → enrich → transform → dispatch` — each step must declare its stage\"\n"}