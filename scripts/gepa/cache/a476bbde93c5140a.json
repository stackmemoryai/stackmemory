{"passed":false,"passRate":0.2,"criteria":{"reads_existing_code":{"passed":false,"quote":"The route is at src/routes/query.js.","reason":"The prompt names the file but does not instruct the agent to READ it first before making changes. It says 'Implement the requested changes' without a prerequisite step of understanding existing code structure, patterns, and dependencies.","feedback":"Add explicit instruction: 'Before writing any code, read src/routes/query.js to understand the existing route structure and patterns.' This ensures the agent grasps the codebase before modifying it."},"apperror_pattern":{"passed":true,"quote":"Use AppError from src/core/errors.js with ErrorCodes.VALIDATION.","reason":"The prompt explicitly names AppError, the correct file path (src/core/errors.js), and the specific error code (ErrorCodes.VALIDATION). This is unambiguous and sufficient for the agent to apply the pattern correctly.","feedback":""},"test_commands":{"passed":false,"quote":"Run lint and tests to verify","reason":"The prompt says 'Run lint and tests' in imperative form but does NOT specify the exact npm commands (npm run lint, npm test, or npm run test:core). An agent may guess the wrong command or use a non-existent script name.","feedback":"Replace with explicit commands: 'Run `npm run lint && npm test` to verify all changes.' This removes ambiguity and ensures the agent runs the correct tooling."},"supertest_pattern":{"passed":false,"quote":"Write or update tests as needed","reason":"The prompt mentions writing tests but does not mention Supertest, does not clarify that tests should use the `app` export (not `server`), and does not explain how to structure route tests in this codebase. An agent may write unit tests that bypass the HTTP layer entirely.","feedback":"Add: 'Write tests using Supertest, importing `app` from the route file and testing the HTTP endpoint. See existing test files for the DI factory test pattern.' This ensures HTTP-layer testing and consistency with codebase conventions."},"di_factory_pattern":{"passed":false,"quote":"","reason":"The prompt does not mention the DI (dependency injection) factory pattern at all. The critical gap is that route files in this codebase use a `createRouter(deps)` factory export pattern, not bare `router.get()` definitions. Without this guidance, the agent will likely write code that doesn't follow the pattern, breaks testability, and fails to integrate with the DI container.","feedback":"Add critical instruction: 'Route files use a DI factory pattern—read the `createRouter(deps)` export in src/routes/query.js and ensure your changes follow this pattern. Do not use bare router.get() calls.' This prevents the most likely architectural error."}},"judgeMode":"llm","duration":27447,"output":"**Prompt Evaluation — STA-600**\n\n| Criterion | Pass? | Notes |\n|---|---|---|\n| Read existing route file first | ✗ | Mentions `src/routes/query.js` but doesn't say *read it first* — agent may write without understanding existing structure |\n| AppError pattern | ✓ | Explicitly names `AppError` + `ErrorCodes.VALIDATION` + file path |\n| `npm run lint` + `npm test` | ~| Says \"Run lint and tests\" but omits exact commands — agent may guess wrong runner |\n| Supertest for route tests | ✗ | No mention; agent might write unit tests that skip HTTP layer entirely |\n| DI factory pattern | ✗ | Critical gap — route files use DI factories; agent will likely write a bare `router.get()` that can't be tested via supertest |\n\n**Summary:** 1/5 fully met, 1/5 partial, 3/5 missing.\n\n**Critical gaps:**\n- Missing \"read `src/routes/query.js` before writing any code\"\n- Missing DI factory requirement (biggest risk — wrong pattern breaks testability)\n- Missing Supertest + `app` (not `server`) guidance\n- Should note `npm test` is long-running → run in background/sub-agent\n\n**Suggested additions:**\n```\nBefore writing: read src/routes/query.js to understand the DI factory export pattern.\nTests must use Supertest with `app` (not `server`) and follow the DI pattern.\nRun: npm run lint && npm run test (in background — long-running).\n```\n"}