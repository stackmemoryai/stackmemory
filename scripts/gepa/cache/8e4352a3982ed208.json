{"passed":false,"passRate":0.2,"criteria":{"reads_existing_code":{"passed":false,"quote":"Step 1 says \"read the issue description\" — doesn't say to read `src/routes/query.js` before implementing","reason":"The prompt instruction list begins with 'Read the issue description carefully' but does not explicitly instruct the agent to read and understand the existing route file at src/routes/query.js before making modifications. This violates the project standard of reading code before writing.","feedback":"Add an explicit instruction before Step 1: 'First, read the existing route file at src/routes/query.js to understand the current implementation and structure.' This ensures the agent understands the existing code patterns before modifying."},"apperror_pattern":{"passed":true,"quote":"Use AppError from src/core/errors.js with ErrorCodes.VALIDATION.","reason":"The prompt explicitly names AppError, specifies the correct source file (src/core/errors.js), and identifies the correct error code (ErrorCodes.VALIDATION). This is sufficient guidance on the error handling pattern.","feedback":""},"test_commands":{"passed":false,"quote":"Step 4 says \"run lint and tests\" but omits exact commands; missing the critical note that `npm test` is long-running and must run in background/sub-agent","reason":"The prompt uses vague language ('run lint and tests') without specifying exact npm commands. It does not distinguish between npm run lint and npm test, nor does it provide warnings about test execution time or the need for background execution. This could lead to the agent running tests synchronously and hanging.","feedback":"Replace Step 4 with specific commands: 'Run `npm run lint` to check code style. Then run `npm test` in the background (or use `npm run test:core` if available) to verify fixes.' Add a note: 'npm test may take time; consider running in a separate session or sub-agent.'"},"supertest_pattern":{"passed":false,"quote":"Step 3 only says \"write or update tests\" — no Supertest mention, no `app` vs `server` distinction","reason":"The prompt provides no guidance on testing the route handler. It does not mention Supertest (the testing framework used in this codebase), does not clarify whether to test with `app` or `server`, and does not specify how to structure the test (e.g., POST /api/v1/query with invalid UUID). This will likely result in incorrect test code.","feedback":"Add to Step 3: 'Update tests in src/routes/__tests__/query.test.js using Supertest. Test the /api/v1/query endpoint with an invalid UUID in conversation_id (e.g., \"not-a-uuid\"). Assert that it returns a 400 status with an AppError response. Use `app` (not `server`) when calling supertest().' Include an example: `supertest(app).post('/api/v1/query').send({conversation_id: 'invalid'}).expect(400)`."},"di_factory_pattern":{"passed":false,"quote":"No mention of DI route factories; a naive fix might add validation inline without respecting the factory pattern","reason":"The prompt makes no reference to the createRouter(deps) dependency injection factory pattern used in this codebase. Without this guidance, an agent might add validation logic in a way that breaks the DI structure, makes testing harder, or does not follow the established pattern.","feedback":"Add context to Step 2: 'The route handler uses a DI factory pattern (createRouter(deps)). Implement UUID validation inside the route handler function, before the database query. Ensure the AppError is thrown and caught by the existing error middleware, not handled inline.'"}},"judgeMode":"llm","duration":33421,"output":"**Prompt Evaluation: STA-600 Fix**\n\n| Criterion | Present? | Notes |\n|---|---|---|\n| Read existing route file first | **No** | Step 1 says \"read the issue description\" — doesn't say to read `src/routes/query.js` before implementing |\n| AppError pattern | **Yes** | Explicitly names `AppError` from `src/core/errors.js` and `ErrorCodes.VALIDATION` |\n| `npm run lint` + `npm test` | **Partial** | Step 4 says \"run lint and tests\" but omits exact commands; missing the critical note that `npm test` is long-running and must run in background/sub-agent |\n| Supertest for route testing | **No** | Step 3 only says \"write or update tests\" — no Supertest mention, no `app` vs `server` distinction |\n| DI factory pattern | **No** | No mention of DI route factories; a naive fix might add validation inline without respecting the factory pattern |\n\n**Additional gaps:**\n- No mention of `catch {}` (not `catch (_err) {}`) ESLint rule — likely to cause a lint failure\n- No \"No Co-Authored-By in commits\" constraint\n- No instruction to read the file before writing (violates the project's read-before-write standard)\n\n**Verdict:** The prompt gets the error handling API right but will likely cause an agent to: skip reading the existing route, write tests incorrectly (server vs app), hang on inline `npm test`, and produce a lint failure on the catch clause. The DI pattern omission is the highest risk — an agent could implement this in a way that breaks testability.\n"}