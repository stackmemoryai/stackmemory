{"passed":false,"passRate":0.25,"criteria":{"ctx_schema":{"passed":false,"quote":"Mentions \"conflicting content\" and \"nodes\" generically, but never names the schema tables","reason":"The AI output explicitly states that ctx_nodes/ctx_edges schema references are absent from the prompt. The prompt template only says 'find ctx_nodes with conflicting content' without specifying table structure, column names, or embedding field details. An implementer would lack critical schema knowledge.","feedback":"The prompt should explicitly list the ctx_nodes schema (id, tenant_id, entity_id, content, embedding, created_at) and ctx_edges schema (id, tenant_id, src_id, dst_id, rel_type). Include example column references so the implementer knows what fields are available for the contradiction_score calculation."},"tenant_scoping":{"passed":false,"quote":"No mention of multi-tenant isolation — critical for a SaaS context graph","reason":"The prompt contains zero reference to tenant_id scoping or multi-tenant isolation. The instruction 'find ctx_nodes with conflicting content' does not enforce or mention that all queries must be scoped by tenant_id. This is a severe security gap; an agent could write queries that leak data across tenants.","feedback":"Add an explicit instruction: 'All queries MUST scope by tenant_id. Never query ctx_nodes without WHERE tenant_id = $1. Ensure the function signature accepts tenant_id as a parameter.' This should be highlighted as a non-negotiable constraint."},"existing_service":{"passed":true,"quote":"Instructs reading existing `graph-query.js` | Yes | \"Read the issue description carefully\" + the stub is named explicitly","reason":"The prompt template includes 'Read the issue description carefully' and explicitly names the stub location as 'src/ctx/graph-query.js'. This satisfies the instruction to read the existing service, though it is somewhat implicit rather than explicit.","feedback":""},"graph_service_wrapper":{"passed":false,"quote":"Not mentioned; agents may bypass it and write raw queries","reason":"The prompt makes no reference to the GraphService wrapper at src/graph/service.ts. Without this guidance, an implementer may write direct database queries or use an incorrect abstraction layer, breaking architectural conventions. The prompt is silent on how to access the graph data layer.","feedback":"Add explicit instruction: 'Use the GraphService wrapper at src/graph/service.ts for all database access. Do not write raw SQL queries. Review how existing query functions in graph-query.js invoke GraphService to understand the pattern.'"}},"judgeMode":"llm","duration":45718,"output":"The repo only contains `CLAUDE.md` — no actual source files exist. Evaluating purely against the prompt template:\n\n**Evaluation: 2/4 conventions missing**\n\n| Convention | Present? | Notes |\n|---|---|---|\n| References `ctx_nodes`/`ctx_edges` schema | No | Mentions \"conflicting content\" and \"nodes\" generically, but never names the schema tables |\n| `tenant_id` scoping | No | No mention of multi-tenant isolation — critical for a SaaS context graph |\n| Instructs reading existing `graph-query.js` | Yes | \"Read the issue description carefully\" + the stub is named explicitly |\n| References `GraphService` wrapper at `src/graph/service.ts` | No | Not mentioned; agents may bypass it and write raw queries |\n\n**Verdict: Fails ProvenantAI conventions.** The prompt is missing 3 of 4 checks:\n\n1. **`ctx_nodes`/`ctx_edges` schema** — absent. An agent implementing `getContradictions()` without schema knowledge may invent column names.\n2. **`tenant_id` scoping** — absent. This is the most dangerous gap; contradiction queries without tenant isolation would leak cross-tenant data.\n3. **`GraphService` wrapper** — absent. Agents will likely write direct DB queries instead of going through the established abstraction layer.\n\n**Recommended additions to the issue instructions:**\n```\n- Schema: ctx_nodes (id, tenant_id, entity_id, content, embedding, created_at), ctx_edges (id, tenant_id, src_id, dst_id, rel_type)\n- All queries MUST scope by tenant_id — never query ctx_nodes without a WHERE tenant_id = $1 clause\n- Use the GraphService wrapper at src/graph/service.ts for DB access, not raw queries\n- Read src/ctx/graph-query.js fully before implementing — note existing query patterns\n```\n"}