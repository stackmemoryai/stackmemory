{"passed":false,"passRate":0,"criteria":{"no_jest_globals_import":{"passed":false,"quote":"The prompt template is missing all 4 conventions.","reason":"The AI output explicitly states that the prompt does not warn against importing from @jest/globals. The evaluation table shows 'Warn against `@jest/globals` import (use globals)' as 'No'. There is no instruction in the prompt template telling the agent to use global jest instead of @jest/globals.","feedback":"Add a section to the prompt instructions: 'Import jest from global scope, not from @jest/globals. Use: `const { describe, it, expect, beforeEach } = global;` or rely on globals being available without explicit import.'"},"clear_all_mocks_warning":{"passed":false,"quote":"The prompt template is missing all 4 conventions. ... `jest.clearAllMocks()` resets `mockReturnValue` → re-set in `beforeEach` | No","reason":"The evaluation explicitly identifies that the prompt lacks guidance on jest.clearAllMocks() behavior. The table shows this convention as absent ('No'). The prompt contains no warning that clearAllMocks() resets mockReturnValue and that return values must be re-configured in beforeEach hooks.","feedback":"Add a section noting: 'When using jest.clearAllMocks() in beforeEach, remember it resets mockReturnValue. Re-set expected return values for each test in beforeEach, not once in beforeAll or at describe scope.'"},"eslint_catch_pattern":{"passed":false,"quote":"does it specify using catch {} not catch (_err) {} for ESLint ... No","reason":"The AI output clearly states this convention is absent from the prompt ('No' in the evaluation table). There is no instruction telling the agent to use empty catch blocks `catch {}` instead of `catch (_err) {}` to comply with ESLint rules.","feedback":"Add to the prompt: 'Use `catch {}` syntax (empty catch block) rather than `catch (_err) {}` to comply with project ESLint rules. Unused error parameters trigger linting failures.'"},"check_existing_patterns":{"passed":false,"quote":"does it tell the agent to check existing test patterns in __tests__/? ... No","reason":"The evaluation table shows this criterion as missing ('No'). The prompt contains only generic boilerplate instructions (read → implement → test → lint → commit) with no directive to examine existing tests in the __tests__/ directory before writing new tests.","feedback":"Add to the instructions: 'Before writing tests, review existing test files in __tests__/ to understand the project's test structure, naming conventions, helper functions, and assertion patterns. Match the established style.'"}},"judgeMode":"llm","duration":27844,"output":"The prompt template is missing all 4 conventions. Evaluation:\n\n| Check | Present? |\n|---|---|\n| Warn against `@jest/globals` import (use globals) | No |\n| `jest.clearAllMocks()` resets `mockReturnValue` → re-set in `beforeEach` | No |\n| `catch {}` not `catch (_err) {}` (ESLint rule) | No |\n| Check existing `__tests__/` patterns before writing | No |\n\nThe prompt only has boilerplate instructions (read → implement → test → lint → commit). It doesn't surface any project-specific testing gotchas from `CLAUDE.md`, so an agent would likely:\n\n- Import `jest` from `@jest/globals`, causing redeclaration errors\n- Set up mocks once in `beforeAll` or `describe` scope and be confused when they don't reset\n- Write `catch (_err) {}` and fail the ESLint pre-commit hook\n- Invent test structure instead of matching existing patterns\n\n**To fix**, the prompt template should inject the relevant CLAUDE.md testing rules section, either verbatim or as a condensed \"Project Conventions\" block appended to the instructions.\n"}