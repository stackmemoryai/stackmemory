{"passed":false,"passRate":0,"criteria":{"model_tiers":{"passed":false,"quote":"Not mentioned; budget thresholds are likely tier-dependent","reason":"The AI output explicitly identifies that model tiers (fast/balanced/deep) are absent from the prompt. The evaluator notes this is a gap because budget overrides 'almost certainly vary by tier' and the agent needs this taxonomy to check the right threshold. The prompt template makes no reference to tier-dependent budget logic.","feedback":"The prompt should include a section stating: 'Check the tenant_budget_overrides table against the query's model tier (fast/balanced/deep) to determine the applicable budget threshold.' This ensures the agent knows budget limits are tier-specific and queries the override table with the correct tier context."},"anthropic_native":{"passed":false,"quote":"Prompt says 'LLM adapter' generically, no mention of Anthropic-native shape","reason":"The AI output identifies that the prompt uses generic language ('LLM adapter') without specifying whether this is an Anthropic-native adapter versus a multi-provider abstraction. The evaluator notes this is critical because the agent may 'write to the wrong interface' if the adapter pattern is ambiguous. The prompt provides no clue about the underlying provider architecture.","feedback":"The prompt should explicitly state: 'This codebase uses an Anthropic-native adapter pattern. Ensure your implementation integrates with the Anthropic-specific usage schema at src/llm/adapter.js (not a multi-provider abstraction).' This grounds the agent in the correct architectural model."},"read_adapter_first":{"passed":false,"quote":"Names the file but says 'add the check after usage recording' — doesn't explicitly say *read first, understand usage shape before writing*","reason":"The prompt names `src/llm/adapter.js` but frames it as a target for insertion ('add the check after usage recording') rather than as a required read-first artifact. The CLAUDE.md conventions mandate a `[VERIFY] read before write` pattern. The prompt provides no explicit instruction to read and understand the adapter's usage recording shape before implementing the budget check.","feedback":"Restructure the instruction as a two-step process: '1. **Read** src/llm/adapter.js to understand how usage is recorded and where the post-recording hook is. 2. **Implement** the budget check after the usage recording point.' This enforces the read-before-write discipline and prevents assumptions about the adapter interface."},"trace_logger":{"passed":false,"quote":"Says 'warning log' but doesn't specify the structured observability mechanism (trace-logger); this is a gap given the Labels include `observability`","reason":"The prompt uses vague language ('warning log') without specifying the structured observability tool. The evaluator correctly notes the `observability` label signals this is a first-class concern, yet the prompt does not reference trace-logger or any structured span/context mechanism. This leaves the agent to guess at the logging pattern.","feedback":"Add explicit guidance: 'Use trace-logger (import from src/observability/trace-logger) to emit a structured span when budget is exceeded. Include tenant_id, query_id, token_count, budget_threshold, and tier in the span context. Do not use console.log or generic warning logs.' This ensures observability is properly instrumented per ProvenantAI conventions."}},"judgeMode":"llm","duration":35104,"output":"The repo only contains `CLAUDE.md` — no source code exists yet. Evaluation is based solely on the CLAUDE.md conventions and the prompt itself.\n\n---\n\n**Evaluation: STA-635 prompt vs. ProvenantAI LLM adapter conventions**\n\n| Check | Present? | Notes |\n|---|---|---|\n| Model tiers (fast/balanced/deep) | **No** | Not mentioned; budget thresholds are likely tier-dependent |\n| Anthropic-native adapter pattern | **No** | Prompt says \"LLM adapter\" generically, no mention of Anthropic-native shape |\n| Instructs reading `src/llm/adapter.js` first | **Partial** | Names the file but says \"add the check after usage recording\" — doesn't explicitly say *read first, understand usage shape before writing* |\n| trace-logger for structured observability | **No** | Says \"warning log\" but doesn't specify the structured observability mechanism (trace-logger); this is a gap given the Labels include `observability` |\n| Cost estimation pattern | **No** | Focuses on token count vs. budget threshold but doesn't reference how cost is estimated (e.g., per-model token pricing, cost estimation utility) |\n\n**Summary of gaps:**\n\n1. **Missing model tier context** — budget overrides almost certainly vary by tier; agent needs to know fast/balanced/deep taxonomy to check the right threshold.\n2. **No Anthropic-native adapter callout** — if this repo uses an Anthropic-native adapter (vs. OpenAI-compat), the agent may write to the wrong interface.\n3. **Weak read-first instruction** — CLAUDE.md mandates `[VERIFY] read before write`; prompt implies familiarity rather than enforcing a read-first step.\n4. **trace-logger omitted** — `observability` label + structured logging = should call out the trace-logger explicitly rather than leaving \"warning log\" ambiguous.\n5. **No cost estimation pattern** — token count alone isn't budget; without specifying the cost-per-token pattern, the agent will likely implement a raw token count check instead of a cost-based one.\n\n**Recommendation:** Add a `## Context` block to the"}