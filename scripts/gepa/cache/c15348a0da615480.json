{"passed":false,"passRate":0,"criteria":{"ctx_schema":{"passed":false,"quote":"Mentions `ctx_nodes` but not `ctx_edges` — contradiction detection likely needs edges to traverse relationships","reason":"The prompt references only `ctx_nodes` in 'find ctx_nodes with conflicting content' but does not mention `ctx_edges` schema. The evaluation output itself identifies this gap as a problem ('contradiction detection likely needs edges to traverse relationships'). For ProvenantAI conventions, both the table schema and the relationship model (edges) should be explicitly referenced to guide correct implementation.","feedback":"The prompt should explicitly state: 'Query ctx_nodes and ctx_edges tables to find nodes with the same entity_id but conflicting claim_type or content values.' This grounds the agent in the actual schema structure needed for contradiction detection."},"tenant_scoping":{"passed":false,"quote":"**Missing** | No mention of multi-tenant isolation; CLAUDE.md lists 'Multi-tenant container isolation' as a key pattern","reason":"The prompt contains zero explicit instruction or reminder about tenant_id scoping. The evaluation output flags this as a critical gap that could result in 'cross-tenant data leak risk.' A ProvenantAI convention-compliant prompt must mandate tenant_id filtering for all graph queries.","feedback":"Add explicit instruction: 'All ctx_nodes queries must filter by tenant_id to ensure multi-tenant isolation. Do not return contradictions across different tenants.' Include a concrete example: `WHERE tenant_id = ? AND entity_id = ?`"},"existing_service":{"passed":false,"quote":"**Missing** | Says the stub exists but doesn't explicitly tell the agent to read the file before implementing","reason":"The prompt states the stub exists at 'src/ctx/graph-query.js' but does not include an explicit 'read-first' instruction. This increases risk of signature misalignment, overwriting important context, or duplicating logic. A strict evaluation requires that agents be directed to audit existing code before modification.","feedback":"Add as step 1 or step 0: 'First, read the full src/ctx/graph-query.js file to understand the getContradictions() stub signature, any helper functions, and existing query patterns. Do not modify without understanding the current interface.'"},"graph_service_wrapper":{"passed":false,"quote":"**Missing** | No mention of the wrapper layer","reason":"The prompt does not reference GraphService at src/graph/service.ts. The evaluation output identifies this as a critical omission because direct DB queries bypass 'provenance tracking.' ProvenantAI conventions require all graph operations to flow through the service wrapper for audit and compliance.","feedback":"Add explicit instruction: 'Use GraphService from src/graph/service.ts for all ctx_nodes and ctx_edges queries. Do not call the database directly. This ensures provenance tracking and multi-tenant isolation are enforced at the service layer.'"}},"judgeMode":"llm","duration":46637,"output":"The working directory only contains `CLAUDE.md` — no source files exist. Evaluation must be based on what the prompt references vs. what the CLAUDE.md establishes as conventions.\n\n---\n\n**Evaluation: STA-650 prompt vs. ProvenantAI conventions**\n\n| Check | Result | Notes |\n|---|---|---|\n| References `ctx_nodes`/`ctx_edges` schema | Partial | Mentions `ctx_nodes` but not `ctx_edges` — contradiction detection likely needs edges to traverse relationships |\n| `tenant_id` scoping | **Missing** | No mention of multi-tenant isolation; CLAUDE.md lists \"Multi-tenant container isolation\" as a key pattern |\n| Instructs reading existing `graph-query.js` | **Missing** | Says the stub exists but doesn't explicitly tell the agent to read the file before implementing |\n| References `GraphService` at `src/graph/service.ts` | **Missing** | No mention of the wrapper layer |\n\n**Gaps that will cause agent drift:**\n\n1. **No tenant scoping** — agent will likely implement `getContradictions()` without `tenant_id` filtering, producing a cross-tenant data leak risk\n2. **No read-first instruction** — agent may overwrite or misalign with existing stub signature/interface\n3. **Missing `GraphService` reference** — agent may call DB directly instead of going through the service layer, bypassing provenance tracking\n4. **No `ctx_edges` mention** — contradiction detection without traversing edges may produce false positives (two nodes about same entity in different contexts)\n\n**Recommended additions to the prompt:**\n```\n- Read src/ctx/graph-query.js in full before implementing\n- All queries must scope by tenant_id (multi-tenant isolation required)\n- Use GraphService at src/graph/service.ts — do not query ctx_nodes/ctx_edges directly\n- Contradiction candidates should be found via ctx_edges (same entity, conflicting claim type)\n```\n"}