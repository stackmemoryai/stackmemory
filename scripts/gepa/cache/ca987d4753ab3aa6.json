{"passed":false,"passRate":0,"criteria":{"no_jest_globals_import":{"passed":false,"quote":"The prompt fails all four checks... Warn against `import from '@jest/globals'` | No","reason":"The AI output explicitly identifies that the prompt lacks any warning against importing from @jest/globals. The prompt template contains generic instructions with no mention of jest import conventions, meaning an agent would default to potentially incorrect patterns like `import { describe, it, expect } from '@jest/globals'` which causes redeclaration errors.","feedback":"Add explicit guidance: 'Do not import from @jest/globals. Use the global jest object: describe, it, expect, beforeEach, etc. are already available globally.' This should appear in the Instructions section before step 2."},"clear_all_mocks_warning":{"passed":false,"quote":"`jest.clearAllMocks()` resets `mockReturnValue` → re-set in `beforeEach` | No","reason":"The prompt contains no mention of jest.clearAllMocks() behavior or the need to re-set mock return values after clearing. The AI output notes this absence and warns that without this guidance, the agent could write flaky tests that fail intermittently due to stale mock state.","feedback":"Add to Instructions: 'When using jest.clearAllMocks() in beforeEach, remember it resets mockReturnValue to undefined. You must re-set mockReturnValue in beforeEach after calling clearAllMocks() if you want mocks to have consistent return values across tests.'"},"eslint_catch_pattern":{"passed":false,"quote":"`catch {}` not `catch (_err) {}` for ESLint | No","reason":"The prompt provides zero guidance on ESLint-compliant error handling patterns. The AI output specifically warns that an agent would write `catch (_err) {}` which violates ESLint rules. The correct pattern `catch {}` is never mentioned.","feedback":"Add to Instructions: 'Follow ESLint conventions for catch blocks: use `catch {}` (empty catch) when the error is intentionally unused. Do not use `catch (_err) {}` with an underscore-prefixed variable name.'"},"check_existing_patterns":{"passed":false,"quote":"Check existing patterns in `__tests__/` | No... instead of matching the codebase pattern","reason":"The prompt provides generic instructions with no directive to examine existing test files before writing new tests. The AI output notes that without this instruction, the agent would 'invent test structure from scratch instead of matching the codebase pattern.' This violates the principle of consistency with project conventions.","feedback":"Add to step 1 or as a new step: 'Before writing tests, examine existing test files in `src/recipes/__tests__/` or the nearest test directory. Match the file naming convention (e.g., `webhook-signature.test.js`), describe block structure, assertion patterns, mock setup, and helper utilities already in use. Do not invent new patterns.'"}},"judgeMode":"llm","duration":35228,"output":"The prompt fails all four checks:\n\n| Check | Present? |\n|-------|----------|\n| Warn against `import from '@jest/globals'` | No |\n| `jest.clearAllMocks()` resets `mockReturnValue` → re-set in `beforeEach` | No |\n| `catch {}` not `catch (_err) {}` for ESLint | No |\n| Check existing patterns in `__tests__/` | No |\n\nThe prompt is a bare issue transcription with generic instructions. It contains zero ProvenantAI-specific conventions. An agent executing this would likely:\n\n- `import { describe, it, expect } from '@jest/globals'` → redeclaration errors\n- Write `catch (_err) {}` → ESLint failure\n- Not re-set mocks after `clearAllMocks()` → flaky tests\n- Invent test structure from scratch instead of matching the codebase pattern\n\nThe `## Instructions` block should inject the relevant CLAUDE.md sections for test tasks — at minimum the **Testing Rules** and **ESLint Rules** blocks, plus a directive to read `src/recipes/__tests__/` (or the nearest sibling test file) before writing anything.\n"}