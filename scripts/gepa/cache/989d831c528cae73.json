{"passed":false,"passRate":0.2,"criteria":{"reads_existing_code":{"passed":false,"quote":"The route is at src/routes/query.js","reason":"The prompt names the file but does not instruct the agent to read it first before making changes. It only mentions where the file is located. There is no explicit directive like 'Read src/routes/query.js before modifying' in the Instructions section.","feedback":"Add an explicit instruction at the beginning of the Instructions block: 'Read src/routes/query.js completely before making any changes to understand the current implementation and structure.'"},"apperror_pattern":{"passed":true,"quote":"Use AppError from src/core/errors.js with ErrorCodes.VALIDATION","reason":"The prompt explicitly references the AppError class and the correct ErrorCodes.VALIDATION constant, specifying both the module path and the pattern to use. This is sufficient guidance for the agent to follow the established error pattern.","feedback":""},"test_commands":{"passed":false,"quote":"Run lint and tests to verify","reason":"Step 4 mentions running 'lint and tests' in generic terms but does not specify the actual npm commands (npm run lint, npm test). The agent may not know the exact command syntax or may run the wrong commands.","feedback":"Specify exact commands: 'Run `npm run lint && npm test` to verify all changes pass linting and tests.'"},"supertest_pattern":{"passed":false,"quote":"Write or update tests as needed","reason":"Step 3 is generic and mentions writing tests but makes no mention of Supertest, how to structure the test, or critical details like passing `app` instead of `server`. The agent could write incorrect tests that don't follow the project's testing pattern.","feedback":"Add guidance: 'Write route tests using Supertest, passing the Express `app` instance (not `server`). Test both valid UUID and invalid UUID inputs to verify the 400 error is returned with AppError.'"},"di_factory_pattern":{"passed":false,"quote":"","reason":"The prompt makes no mention of the DI factory pattern at all. There is no instruction to export a function that accepts dependencies (like `db`) and returns a router. This is the highest risk criterion—the agent may write a plain route handler without the factory wrapper, making dependency injection impossible.","feedback":"Add explicit instruction: 'Routes in this codebase use a DI factory pattern. Export a function like `module.exports = (db) => { const router = Router(); router.get(...); return router; }` so dependencies can be injected during tests and startup.'"}},"judgeMode":"llm","duration":31752,"output":"## Prompt Evaluation: STA-600\n\n| Criterion | Present | Notes |\n|---|---|---|\n| Read existing route file first | Partial | Names `src/routes/query.js` in description but no explicit \"read before editing\" instruction |\n| AppError pattern | Yes | \"Use AppError from src/core/errors.js with ErrorCodes.VALIDATION\" — explicit |\n| `npm run lint` + `npm test` commands | Partial | Step 4 says \"Run lint and tests\" but omits exact commands |\n| Supertest for route testing | No | Step 3 says \"Write or update tests as needed\" — no Supertest mention |\n| DI factory pattern | No | Completely absent — agent may write a plain `router.get(...)` instead of a factory |\n\n**Summary: 1.5/5 criteria fully met**\n\n**Critical gaps:**\n\n1. **No read-first guard** — agent may overwrite without reading. Add: *\"Read `src/routes/query.js` before making any changes.\"*\n2. **Missing DI factory pattern** — highest risk. Without it, tests can't inject mock DB. Add: *\"Routes use DI factory pattern — export a function that accepts `db` and returns a router.\"*\n3. **No Supertest callout** — agent may skip integration test or write unit test against wrong surface. Add: *\"Use Supertest with `app` (not `server`) for route tests.\"*\n4. **Vague test command** — add: *\"Run `npm run lint && npm test` to verify.\"*\n\n**Suggested additions to Instructions block:**\n```\n1. Read src/routes/query.js before modifying it.\n4. Run `npm run lint` then `npm test` to verify.\n   - Use Supertest (pass `app`, not `server`)\n   - Follow the DI factory pattern: export fn(db) → router\n```\n"}