{"passed":false,"passRate":0.5,"criteria":{"mentions_lint_test":{"passed":true,"quote":"4. Run lint and tests to verify","reason":"The prompt explicitly instructs the agent to run lint and tests. However, the AI output correctly notes that specific commands are not provided. Step 4 mentions the action but not the commands themselves. This satisfies the criterion at a basic level—the agent is directed to perform these steps—but lacks specificity on how to invoke them.","feedback":"Include specific commands: 'Run `npm run lint` and `npm test` to verify your changes pass all checks.'"},"commit_format":{"passed":false,"quote":"5. Commit your changes with a descriptive message","reason":"The prompt uses the term 'descriptive message' which is vague and provides no format specification. The AI output correctly identifies this as missing conventional commit format (type(scope): message). The prompt does not guide the agent toward any standard like Conventional Commits.","feedback":"Specify commit format explicitly: 'Commit with message format: `fix(sqlite-adapter): return undefined instead of throwing on deleted frame lookup`' or link to your project's commit convention."},"bug_context":{"passed":false,"quote":"When a frame is deleted and then looked up by ID, the sqlite adapter throws an unhandled null reference instead of returning undefined. Stack trace attached.","reason":"The prompt describes the symptom (throws instead of returns undefined) and references a stack trace, but the stack trace is not included in the output. The AI output flags this critical gap: 'Stack trace referenced (\"attached\") but not included — agent can't reproduce without it.' Without the actual stack trace and file location, the agent cannot efficiently identify where the bug occurs.","feedback":"Include the actual stack trace in the prompt and specify the file path (e.g., 'src/adapters/sqlite.js, line 147 in lookupFrameById()') so the agent can directly locate and understand the null dereference."},"validation_step":{"passed":true,"quote":"4. Run lint and tests to verify","reason":"The prompt includes step 4 which instructs the agent to run lint and tests as a validation mechanism. This serves as a verification step to ensure the fix does not break existing functionality. However, the validation is generic and does not specify what passing behavior should look like (e.g., 'test should assert lookupById(deletedId) returns undefined').","feedback":"Enhance validation by adding: 'Add a test case: `expect(adapter.lookupFrameById(deletedFrameId)).toBeUndefined()` and ensure all existing tests pass.'"}},"judgeMode":"llm","duration":25639,"output":"**Present:**\n- Bug description (what fails and when)\n- Instructions to lint/test\n- Instructions to write/update tests\n- Commit message instruction (generic)\n- Working directory context\n\n**Missing:**\n\n| Category | Gap |\n|---|---|\n| **Lint/test commands** | No commands given. Agent must discover them (this repo has `npm run lint` / `npm test` — not obvious without reading CLAUDE.md) |\n| **Commit format** | \"descriptive message\" is vague — missing `type(scope): message` convention and branch prefix requirement |\n| **Error handling expectation** | No mention of \"return `undefined` instead of throwing\" — agent might add a try/catch that swallows or rethrows |\n| **Reproduction context** | Stack trace referenced (\"attached\") but not included — agent can't reproduce without it |\n| **File/code location** | No pointer to the sqlite adapter file or the frame lookup function — agent must search |\n| **Definition of done** | No assertion about what correct behavior looks like (e.g., `lookupById(deletedId)` returns `undefined`, not `null` or throws) |\n\n**Verdict:** Marginally adequate. An agent with access to CLAUDE.md can infer commands and conventions, but the missing stack trace is a real blocker — the agent will have to grep speculatively for the null dereference. Adding the stack trace, the exact file path, and an explicit \"return `undefined`, do not throw\" directive would make this actionable without guesswork.\n"}