{"passed":false,"passRate":0,"criteria":{"auth_middleware_reference":{"passed":false,"quote":"Names `authenticateAny` middleware but omits the file path","reason":"The issue description mentions 'authenticateAny middleware' in prose but does not explicitly reference the file path 'src/auth/auth.middleware.js'. The AI output correctly identifies this gap—the middleware is named but not formally grounded to its source file location.","feedback":"Add explicit reference: 'This endpoint must use the authenticateAny middleware from src/auth/auth.middleware.js to support both Clerk and API key authentication.'"},"three_auth_modes":{"passed":false,"quote":"Only mentions Clerk + API key — test mode omitted","reason":"The issue states 'Must work with both Clerk auth and API key auth' but makes no mention of test mode auth path. ProvenantAI conventions require all three modes (Clerk, API key, test mode) to be explicitly documented. Test mode is a critical auth path that must be handled.","feedback":"Add: 'Must support three authentication modes: (1) Clerk auth, (2) API key auth, and (3) test mode (which bypasses auth verification in test environments).'"},"tenant_isolation":{"passed":false,"quote":"Says 'authenticated tenant' but doesn't explicitly require scoping/filtering by `tenant_id` in queries","reason":"The issue uses the term 'authenticated tenant' descriptively but does not explicitly mandate that all database queries and key operations must be scoped by tenant_id. Multi-tenant isolation requires explicit, repeated emphasis in implementation requirements to prevent cross-tenant data leakage.","feedback":"Add: 'All database queries and key lookups must be filtered by tenant_id of the authenticated user. The old key and new key must both be scoped to prevent cross-tenant key rotation.'"},"credential_encryption":{"passed":false,"quote":"No mention of encryption standard for storing the new key","reason":"The issue completely omits encryption requirements for the new API key before database storage. ProvenantAI conventions mandate AES-256-GCM encryption for all credential storage. This is a security-critical detail that must be explicit in the issue specification.","feedback":"Add: 'The new API key must be encrypted using AES-256-GCM before being written to the database. Use the KMS encryption utility from the credential storage module.'"}},"judgeMode":"llm","duration":38926,"output":"The working directory only contains `CLAUDE.md` — no source code present. Evaluating purely against the issue text:\n\n---\n\n**Evaluation: STA-625 vs ProvenantAI auth conventions**\n\n| Check | Status | Notes |\n|---|---|---|\n| References `src/auth/auth.middleware.js` | **FAIL** | Names `authenticateAny` middleware but omits the file path |\n| Three auth modes (Clerk + API key + test mode) | **FAIL** | Only mentions Clerk + API key — test mode omitted |\n| Multi-tenant isolation (scope by `tenant_id`) | **PARTIAL** | Says \"authenticated tenant\" but doesn't explicitly require scoping/filtering by `tenant_id` in queries |\n| AES-256-GCM for credential encryption | **FAIL** | No mention of encryption standard for storing the new key |\n\n---\n\n**Summary:** Issue passes intent but fails specificity on 3 of 4 checks. Recommended additions before implementation:\n\n1. Add explicit file ref: `authenticateAny` is defined in `src/auth/auth.middleware.js`\n2. Add: \"must handle test mode (bypass auth in test env)\"\n3. Add: \"all DB queries must filter by `tenant_id` — never operate cross-tenant\"\n4. Add: \"new key must be stored encrypted with AES-256-GCM before DB write\"\n"}