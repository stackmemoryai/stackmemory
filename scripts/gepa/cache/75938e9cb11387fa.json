{"passed":false,"passRate":0.2,"criteria":{"reads_existing_code":{"passed":false,"quote":"The route is at src/routes/query.js.","reason":"The prompt names the file but does not instruct the agent to read it first. The evaluation output itself notes 'Names the file path but doesn't instruct agent to **read it before editing**'. Without an explicit 'Read src/routes/query.js in full before making changes', the agent may proceed from assumptions about middleware, error handling shape, or existing context handlers, leading to missed dependencies or incorrect refactoring.","feedback":"Add a directive: 'Read src/routes/query.js in full before making any changes to understand the current error handling and middleware chain.'"},"apperror_pattern":{"passed":true,"quote":"Use AppError from src/core/errors.js with ErrorCodes.VALIDATION.","reason":"The prompt explicitly names the AppError class, specifies the import path (src/core/errors.js), and identifies the correct ErrorCode (VALIDATION). This gives the agent a clear, unambiguous pattern to follow.","feedback":""},"test_commands":{"passed":false,"quote":"Run lint and tests to verify","reason":"The prompt uses vague language ('lint and tests') without specifying the exact npm commands. The evaluation output notes 'should be `npm run lint && npm run test:run`' and warns that `npm test` runs watch mode, which would hang the agent. The prompt does not disambiguate between npm test (watch) and npm run test:run (one-shot).","feedback":"Replace 'Run lint and tests to verify' with 'Run `npm run lint && npm run test:run` to verify your changes' to eliminate ambiguity and prevent the agent from running watch mode."},"supertest_pattern":{"passed":false,"quote":"Write or update tests as needed","reason":"The prompt provides no guidance on the testing framework or pattern. It does not mention Supertest, does not instruct the agent to use `request(app)` vs. mocking req/res directly, and does not specify that route tests should be integration tests. This leaves the agent free to write unit tests with mocked dependencies rather than end-to-end route tests that would catch the actual crash.","feedback":"Add: 'Tests should use Supertest with `import request from 'supertest'` to write integration tests that send actual HTTP requests to the app. Example: `request(app).get('/api/v1/query?conversation_id=invalid').expect(400).'"},"di_factory_pattern":{"passed":false,"quote":"","reason":"The prompt makes no mention of the DI factory pattern. If routes in this codebase use a factory pattern like `export function makeQueryRouter(deps: RouterDeps)` to receive dependencies (db, logger, etc.), the agent has no guidance on preserving this signature or wiring dependencies correctly. The agent may inject dependencies directly as imports or create a new route file that breaks the existing factory pattern.","feedback":"Add: 'Routes in this codebase use a factory pattern: check the current route signature and preserve `export function makeQueryRouter(deps)`. Wire the AppError validation logic within this factory, not as a standalone middleware.'"}},"judgeMode":"llm","duration":32252,"output":"**Prompt Evaluation: STA-600 Fix**\n\n| Criterion | Present? | Notes |\n|---|---|---|\n| Guide to read route file first | Partial | Names the file path (`src/routes/query.js`) but doesn't instruct agent to **read it before editing** |\n| AppError pattern | Yes | Explicitly cites `AppError` + `ErrorCodes.VALIDATION` with correct import path |\n| Exact lint/test commands | No | Says \"run lint and tests\" — vague; should be `npm run lint && npm run test:run` (per CLAUDE.md) |\n| Supertest for route testing | No | \"Write or update tests as needed\" — no mention of test framework or request harness |\n| DI factory pattern | No | Silent on how routes receive dependencies — agent may inject directly or use wrong pattern |\n\n**Verdict: 2/5 criteria met**\n\n**Gaps that will cause failures:**\n\n1. **No \"read first\" instruction** — agent will likely write from assumption, missing existing middleware, error handling shape, or middleware order.\n2. **Vague test instruction** — without Supertest guidance, agent may write unit tests that mock `res/req` directly instead of integration tests that catch the actual crash path.\n3. **Missing DI pattern** — if routes use a factory like `makeQueryRouter(db, logger)`, an agent flying blind will wire dependencies incorrectly or add imports that break the factory.\n4. **Wrong test command** — `npm test` runs watch mode in this repo; `npm run test:run` is the one-shot command. Wrong command = agent hangs.\n\n**Suggested additions to the template:**\n\n```markdown\n- Read {{FILE_PATH}} in full before making any changes\n- Routes use a factory pattern: `export function makeRouter(deps: RouterDeps)` — preserve this signature\n- Tests use Supertest: `import request from 'supertest'` — write integration tests against the real app\n- Commands: `npm run lint` then `npm run test:run` (not `npm test` — that runs watch mode)\n```\n"}