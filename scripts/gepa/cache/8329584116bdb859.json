{"passed":false,"passRate":0.2,"criteria":{"reads_existing_code":{"passed":false,"quote":"Step 1 says \"read the issue description\" — not the source file. `src/routes/query.js` is named but not instructed to be read before editing.","reason":"The prompt lists 'Read the issue description carefully' as step 1, but does not explicitly instruct the agent to read src/routes/query.js or src/core/errors.js before making modifications. Naming a file is not the same as instructing the agent to study it first. An agent following this prompt could begin editing without understanding the existing structure, leading to hallucinated code or incompatible changes.","feedback":"Add explicit step: 'First, read src/routes/query.js and src/core/errors.js to understand the current route structure and error pattern.' Place this before the implementation steps."},"apperror_pattern":{"passed":true,"quote":"Use AppError from src/core/errors.js with ErrorCodes.VALIDATION.","reason":"The prompt explicitly names AppError, specifies the import path (src/core/errors.js), and identifies the correct error code (ErrorCodes.VALIDATION). This is sufficient guidance for an agent to locate and use the pattern correctly.","feedback":""},"test_commands":{"passed":false,"quote":"Step 4 says \"run lint and tests\" but omits exact commands. Given `npm test` is long-running (background process required per CLAUDE.md), the omission matters.","reason":"The prompt uses vague language ('run lint and tests') without specifying the exact npm commands to execute. An agent may guess or use non-standard commands. Additionally, the prompt does not mention that npm test is long-running and requires special handling (background process), which per context is critical knowledge.","feedback":"Specify exact commands: 'Run: `npm run lint` then `npm test` (note: npm test is long-running; run it in the background or in a separate terminal).'"},"supertest_pattern":{"passed":false,"quote":"Step 3 is \"write or update tests as needed\" — no mention of Supertest, no `app` vs `server` distinction (a known footgun per CLAUDE.md).","reason":"The prompt instructs writing tests but provides zero guidance on the testing framework or pattern. It does not mention Supertest, does not clarify that the route requires passing `app` (not `server`), and does not explain how to instantiate the route for testing. This is a critical gap because the distinction between `app` and `server` is explicitly called out as a common failure mode.","feedback":"Add guidance: 'Write tests using Supertest. Pass the route factory an injected `app` object (not `server`). Example: `const route = createQueryRoute(deps); route(app);` then test with `request(app).get(...)`'"},"di_factory_pattern":{"passed":false,"quote":"Not mentioned at all.","reason":"The prompt makes no reference to the DI factory pattern (createRouter, dependency injection, factory functions, etc.). Since the task involves modifying a route file in a codebase that uses dependency injection, an agent unfamiliar with this pattern will either bypass DI entirely, write tests that fail to instantiate the route correctly, or require global mocks (which are forbidden). This is a complete gap.","feedback":"Add guidance: 'The route uses a createRouter(deps) factory pattern for dependency injection. When writing tests, instantiate the route by passing a mocked dependencies object to this factory, then attach it to the test app.'"}},"judgeMode":"llm","duration":42457,"output":"**Prompt Evaluation: STA-600 Bug Fix**\n\n| Criterion | Pass? | Notes |\n|---|---|---|\n| Guide reading route file first | FAIL | Step 1 says \"read the issue description\" — not the source file. `src/routes/query.js` is named but not instructed to be read before editing. |\n| AppError pattern mentioned | PASS | Specifies `AppError` from `src/core/errors.js` + `ErrorCodes.VALIDATION` — sufficient. |\n| `npm run lint` + `npm test` commands | PARTIAL | Step 4 says \"run lint and tests\" but omits exact commands. Given `npm test` is long-running (background process required per CLAUDE.md), the omission matters. |\n| Supertest for route testing | FAIL | Step 3 is \"write or update tests as needed\" — no mention of Supertest, no `app` vs `server` distinction (a known footgun per CLAUDE.md). |\n| DI factory pattern for routes | FAIL | Not mentioned at all. Without this, an agent will likely write tests that bypass DI, either failing or requiring global mocks (explicitly forbidden). |\n\n**Summary:** 1.5/5 criteria met. The three gaps that will most likely cause agent failure:\n\n1. No instruction to read the file before editing → agent may hallucinate the existing structure\n2. No Supertest/`app` guidance → test boilerplate will be wrong\n3. No DI factory mention → tests won't match the route's actual constructor signature\n\n**Suggested additions:**\n```\n- Read src/routes/query.js and src/core/errors.js before making changes\n- Tests must use Supertest with `app` (not `server`); inject deps via the route factory\n- Run: `npm run lint` then `npm test` (run test in background — it's long-running)\n```\n"}