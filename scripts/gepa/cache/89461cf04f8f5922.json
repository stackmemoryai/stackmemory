{"passed":false,"passRate":0,"criteria":{"ctx_schema":{"passed":false,"quote":"Prompt says \"ctx_nodes with conflicting content\" but never mentions `ctx_edges` or the schema structure","reason":"The AI output correctly identifies that while `ctx_nodes` is mentioned in passing within the original prompt (\"find ctx_nodes with conflicting content\"), there is no explicit reference to the `ctx_nodes`/`ctx_edges` schema structure. The prompt does not instruct the agent to understand the relational schema or how edges connect nodes. A proper prompt for a graph query service must ground the implementation in the actual data model.","feedback":"The prompt should explicitly state: 'Review the ctx_nodes and ctx_edges schema in src/ctx/schema.js. Contradictions occur when ctx_nodes linked by ctx_edges (same entity_id) contain semantically conflicting claims. Traverse edges to identify contradiction patterns.'"},"tenant_scoping":{"passed":false,"quote":"No mention of multi-tenant isolation — critical gap for a query service","reason":"The original prompt contains zero references to `tenant_id` filtering or multi-tenant isolation. The AI output rightly flags this as a security/data isolation concern. An agent implementing this stub without explicit tenant scoping instructions could accidentally expose contradictions across tenants or fail to filter results, leading to data leakage in a multi-tenant system.","feedback":"The prompt must include: 'All graph queries must filter by tenant_id. Implement getContradictions(tenant_id, entity_id) to ensure queries return only contradictions within the requesting tenant's scope. Never expose cross-tenant data.'"},"existing_service":{"passed":false,"quote":"Mentions the file path but says \"stub\" without directing the agent to read it first","reason":"The prompt references 'src/ctx/graph-query.js' and calls getContradictions() a 'stub', but does not explicitly instruct the agent to read the existing service file first to understand the current API surface, existing helper functions, and architectural patterns already in use. This is a partial mention at best—the prompt should prioritize reading existing code before implementation.","feedback":"The prompt should begin with: 'First, read src/ctx/graph-query.js in full to understand the current service structure, existing query patterns, and helper functions. The getContradictions() stub is defined there. Build on the existing architecture.'"},"graph_service_wrapper":{"passed":false,"quote":"Not referenced at all","reason":"The original prompt makes no mention of `src/graph/service.ts` or any `GraphService` wrapper. The AI output correctly identifies this as a missing convention—if a canonical `GraphService` wrapper exists as the entry point for graph operations, the prompt should direct the agent to use it rather than implement raw queries. This ensures consistency with existing patterns and avoids duplicate query logic.","feedback":"The prompt should include: 'Use the GraphService wrapper at src/graph/service.ts for all graph operations. Do not write raw database queries. Check how other methods in graph-query.js use GraphService to maintain consistency.'"}},"judgeMode":"llm","duration":38613,"output":"The repo only contains `CLAUDE.md` — no source files exist. Evaluating against conventions based on the prompt text alone:\n\n---\n\n**Evaluation: STA-650 prompt vs ProvenantAI context graph conventions**\n\n| Convention | Present? | Notes |\n|---|---|---|\n| `ctx_nodes`/`ctx_edges` schema reference | **No** | Prompt says \"ctx_nodes with conflicting content\" but never mentions `ctx_edges` or the schema structure |\n| `tenant_id` scoping | **No** | No mention of multi-tenant isolation — critical gap for a query service |\n| Instructs reading existing `graph-query.js` | Partial | Mentions the file path but says \"stub\" without directing the agent to read it first |\n| `GraphService` wrapper (`src/graph/service.ts`) | **No** | Not referenced at all |\n\n**Summary:** 3 of 4 conventions are missing or partial. The prompt skips tenant scoping (a security/isolation concern), omits the `ctx_edges` schema (needed for graph traversal), and doesn't reference the `GraphService` wrapper that should be the canonical entry point. An agent following this prompt would likely implement contradictions as a raw `ctx_nodes`-only query, missing edge relationships and potentially leaking cross-tenant data.\n\n**Recommended additions:**\n1. `Read src/graph/service.ts — use GraphService, not raw DB queries`\n2. `All queries must filter by tenant_id`\n3. `Contradictions may involve ctx_edges (same entity, conflicting claims across linked nodes)`\n"}