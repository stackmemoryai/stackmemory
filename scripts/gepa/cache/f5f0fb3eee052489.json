{"passed":false,"passRate":0,"criteria":{"trace_logger_reference":{"passed":false,"quote":"References `src/utils/trace-logger.js` | **Partial** — file path cited but file doesn't exist in repo; no link to canonical implementation","reason":"The issue mentions trace-logger by file path, but the AI output reveals the file doesn't actually exist in the repository. A reference without a working implementation or canonical documentation is insufficient for ProvenantAI conventions. The agent has no concrete utility to import and use.","feedback":"The issue should either: (1) include the implementation of src/utils/trace-logger.js as part of scope, (2) provide a link to canonical documentation with the exact API signature, or (3) explicitly state that trace-logger.js must be created first. Currently, an agent implementing this would have to invent the API, violating convention consistency."},"trace_span_pattern":{"passed":false,"quote":"Shows `traceSpan` pattern | **No** — issue names the concept but provides no usage example or API signature","reason":"The issue description mentions 'trace-logger spans' in prose but provides zero code examples showing how to call the tracing function. Without a usage pattern like `traceLogger.traceSpan(name, metadata)`, agents must guess the correct API, leading to inconsistent implementations across the codebase.","feedback":"Add a code example to the issue instructions showing the exact pattern, e.g.: `const result = await traceLogger.traceSpan('step-execution', { stepName, duration, success }, () => executeStep(...))`. This removes ambiguity and ensures all implementations follow the same convention."},"no_pii_logging":{"passed":false,"quote":"Instructs NOT to log PII | **No** — omitted entirely","reason":"The issue requests logging 'input/output token counts for LLM steps' but contains zero guidance on excluding PII. LLM step inputs/outputs commonly include user emails, API keys, prompt content, or sensitive data. Without explicit instruction to exclude PII, agents may inadvertently log sensitive information in span metadata.","feedback":"Add a bullet point to the instructions: 'Do NOT log user inputs, API tokens, or sensitive data in span metadata. Only log sanitized fields: step name, duration, success status, and token *counts* (not content). If input/output contains PII, use a hash or generic placeholder instead.'"},"structured_metadata":{"passed":false,"quote":"Structured spans with metadata | **Partial** — lists fields (name, duration, success/failure, token counts) but no schema/shape defined","reason":"The issue specifies desired fields (name, duration, success/failure, token counts) but does not define the structure or schema. It's unclear whether these are top-level span properties, nested in a `metadata` object, or distributed across `tags` and `attributes`. Without a schema, implementations will diverge (some use camelCase, others snake_case; some nest properties, others flatten them).","feedback":"Define a concrete metadata schema in the issue, e.g.: `{ stepName: string, duration: number (ms), success: boolean, inputTokens?: number, outputTokens?: number }` and specify where each field lives in the span object. Include an example: `traceLogger.traceSpan('recipe-step', { stepName: 'validate', duration: 45, success: true, outputTokens: 128 }, ...)`"}},"judgeMode":"llm","duration":32494,"output":"The repository is essentially empty — only `CLAUDE.md` exists, no source files at all.\n\n## Evaluation: STA-660 vs ProvenantAI Observability Conventions\n\n| Check | Result |\n|---|---|\n| References `src/utils/trace-logger.js` | **Partial** — file path cited but file doesn't exist in repo; no link to canonical implementation |\n| Shows `traceSpan` pattern | **No** — issue names the concept but provides no usage example or API signature |\n| Instructs NOT to log PII | **No** — omitted entirely |\n| Structured spans with metadata | **Partial** — lists fields (name, duration, success/failure, token counts) but no schema/shape defined |\n\n### Verdict: **Fails** ProvenantAI conventions\n\nMissing elements the issue should include:\n\n1. **`traceSpan` usage example** — agents need the exact call signature to avoid inventing their own\n2. **PII exclusion directive** — inputs/outputs to LLM steps may contain user data; must be explicitly excluded from span payloads\n3. **Structured metadata schema** — which fields go in `metadata` vs top-level span vs tags; without this, implementations diverge across the codebase\n4. **`trace-logger.js` doesn't exist** — issue references a utility that hasn't been built; either the file needs to be created first or the issue should include its implementation as part of scope\n"}