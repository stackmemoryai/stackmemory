{"passed":false,"passRate":0,"criteria":{"auth_middleware_reference":{"passed":false,"quote":"mentions `authenticateAny middleware` by name only | No file path given","reason":"The issue description states 'authenticateAny middleware' but does not reference the specific file path 'src/auth/auth.middleware.js'. The AI output correctly identifies this as a partial gap—the middleware is named but not localized to its actual file location, which could lead to ambiguity about where to import it from.","feedback":"The issue should explicitly state: 'Use the authenticateAny middleware from src/auth/auth.middleware.js' to ensure the implementer knows the exact import location and file structure."},"three_auth_modes":{"passed":false,"quote":"Three auth modes (Clerk + API key + **test mode**) | Missing — only two modes listed | Test mode not mentioned","reason":"The issue description only mentions 'Clerk auth and API key auth' but omits test mode entirely. The AI output correctly identifies that test mode is absent. ProvenantAI conventions require all three modes (Clerk, API key, and test mode bypass) to be explicitly called out to prevent incomplete implementation that fails in test environments.","feedback":"The issue should be updated to read: 'Must work with Clerk auth, API key auth, and test mode (via authenticateAny middleware)' to ensure the implementer handles all three authentication paths."},"tenant_isolation":{"passed":false,"quote":"Multi-tenant isolation (scope by `tenant_id`) | Weak — \"authenticated tenant\" implied but not explicit | No `tenant_id` scoping requirement stated","reason":"The issue uses vague language ('authenticated tenant') but does not explicitly require scoping by `tenant_id`. This is a critical security gap—without an explicit mandate to scope all DB queries to `req.tenantId`, an implementer might forget this check, risking cross-tenant key exposure. The AI output correctly flags this as weak rather than absent.","feedback":"The issue must explicitly state: 'Scope all database operations to the authenticated tenant's tenant_id. Verify req.tenantId matches the key owner before any read/write/delete operation to prevent cross-tenant access.'"},"credential_encryption":{"passed":false,"quote":"AES-256-GCM for credential encryption | Absent | No encryption guidance at all","reason":"The issue description contains zero mention of encryption, KMS, or AES-256-GCM. This is a complete absence—not even a weak reference. Storing API keys in plaintext or with weak encryption violates ProvenantAI security conventions. An implementer following only the issue text would likely store the new key unencrypted.","feedback":"The issue must include: 'Encrypt the new API key using AES-256-GCM before persisting to the database. Use the KMS module for key encryption/decryption.' This prevents plaintext key storage and enforces proper credential protection."}},"judgeMode":"llm","duration":38390,"output":"The project has no source files — only `CLAUDE.md`. Evaluating the issue text against the stated conventions:\n\n| Convention | Issue Coverage | Gap |\n|---|---|---|\n| `src/auth/auth.middleware.js` path | Partial — mentions `authenticateAny middleware` by name only | No file path given |\n| Three auth modes (Clerk + API key + **test mode**) | Missing — only two modes listed | Test mode not mentioned |\n| Multi-tenant isolation (scope by `tenant_id`) | Weak — \"authenticated tenant\" implied but not explicit | No `tenant_id` scoping requirement stated |\n| AES-256-GCM for credential encryption | Absent | No encryption guidance at all |\n\n**Summary:** The issue covers 1.5 of 4 conventions. Key gaps:\n\n- **Test mode** is omitted — an agent implementing this may skip test-mode bypass handling\n- **`tenant_id` scoping** is not explicit — risks cross-tenant key exposure if not enforced\n- **Encryption requirement** is absent — new key may be stored or returned without AES-256-GCM protection\n\nRecommended additions to the issue description:\n```\n- Use authenticateAny from src/auth/auth.middleware.js (supports Clerk, API key, and test mode)\n- Scope all DB operations to req.tenantId — never touch another tenant's keys\n- Encrypt stored key material with AES-256-GCM before persisting\n```\n"}