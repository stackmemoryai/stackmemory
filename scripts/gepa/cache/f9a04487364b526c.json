{"passed":false,"passRate":0,"criteria":{"ctx_schema":{"passed":false,"quote":"Mentions \"ctx_nodes with conflicting content\" once, but never references `ctx_edges` or the full schema","reason":"The output identifies that the prompt mentions ctx_nodes only in passing (\"ctx_nodes with conflicting content for the same entity\") but never specifies the actual schema columns, relationships, or references ctx_edges at all. A proper convention-compliant prompt should explicitly detail the table structure (entity_id, content, created_at, etc.) and how ctx_nodes and ctx_edges relate.","feedback":"The prompt must explicitly state the ctx_nodes and ctx_edges schema. Include column names, data types, and relationships. Example: 'ctx_nodes(id, tenant_id, entity_id, content, embedding, created_at) and ctx_edges(source_id, target_id, relation_type)'. This prevents the agent from guessing the schema."},"tenant_scoping":{"passed":false,"quote":"Zero mention — contradiction detection without tenant isolation is a multi-tenant bug waiting to happen","reason":"The output correctly identifies that the original prompt contains no reference to tenant_id, org_id, or any multi-tenant scoping mechanism. For a SaaS system, this is a critical security and correctness defect. The prompt must explicitly require filtering by tenant context.","feedback":"The prompt must include: 'All queries must filter by tenant_id to prevent cross-tenant data leakage. The function signature should accept tenant_id as a parameter.' Add an explicit line: 'Ensure contradiction detection is scoped to the requesting tenant only.'"},"existing_service":{"passed":false,"quote":"names the file but says 'stub', not 'read it first' | Doesn't explicitly direct the agent to read before writing","reason":"The prompt references 'src/ctx/graph-query.js' and mentions a 'getContradictions() stub' but does not explicitly instruct the agent to read the existing service structure, dependencies, and patterns before implementing. This creates risk of re-implementing or missing context.","feedback":"Add a step before implementation: '1. Read src/ctx/graph-query.js to understand the existing query patterns, helper functions, and module exports. Identify how other methods handle tenant scoping and graph traversal.' This ensures the agent learns the codebase conventions first."},"graph_service_wrapper":{"passed":false,"quote":"Completely absent; agent will likely bypass the wrapper and write raw queries","reason":"The output notes that GraphService at src/graph/service.ts is not mentioned anywhere in the prompt. This is a critical architectural defect—the agent is likely to write raw database queries instead of using the provenance-tracking wrapper that is stated as a core pattern.","feedback":"Add explicit instruction: 'Use the GraphService wrapper at src/graph/service.ts for all graph queries to ensure provenance tracking. Do not write raw SQL or database queries. Reference how existing methods in graph-query.js invoke GraphService.' This enforces architectural consistency."}},"judgeMode":"llm","duration":32959,"output":"The repo has no `src/` directory — only `CLAUDE.md` exists. Evaluating purely against the prompt text and stated conventions:\n\n---\n\n**Evaluation: STA-650 prompt vs ProvenantAI context graph conventions**\n\n| Convention | Present? | Notes |\n|---|---|---|\n| `ctx_nodes`/`ctx_edges` schema reference | **No** | Mentions \"ctx_nodes with conflicting content\" once, but never references `ctx_edges` or the full schema |\n| `tenant_id` scoping | **No** | Zero mention — contradiction detection without tenant isolation is a multi-tenant bug waiting to happen |\n| Instructs reading existing `graph-query.js` | **Partial** — names the file but says \"stub\", not \"read it first\" | Doesn't explicitly direct the agent to read before writing |\n| References `GraphService` wrapper at `src/graph/service.ts` | **No** | Completely absent; agent will likely bypass the wrapper and write raw queries |\n\n**Missing from the prompt:**\n\n1. No schema shape — agent must guess column names for `ctx_nodes` (entity_id, content, created_at, etc.)\n2. No tenant scoping — `getContradictions()` must filter by `tenant_id` or `org_id`; omitting this is a correctness/security defect\n3. No instruction to use `GraphService` — direct DB access bypasses provenance tracking, which is a stated core pattern in the CLAUDE.md\n4. Semantic similarity is undefined — no embedding model reference, no similarity threshold, no note on whether this is cosine distance on pgvector or a heuristic\n\n**Verdict:** The prompt is insufficient for safe, convention-compliant implementation. An agent following it will likely produce a bare SQL query missing tenant scoping and GraphService integration.\n"}