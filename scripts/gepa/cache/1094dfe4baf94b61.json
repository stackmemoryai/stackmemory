{"passed":false,"passRate":0,"criteria":{"vite_base_path":{"passed":false,"quote":"An agent following it would likely: Build without the `base='/app/'` path prefix → broken asset URLs in production","reason":"The AI output explicitly identifies that the prompt template does NOT mention Vite base='/app/' or BrowserRouter basename='/app'. The output predicts this will cause broken asset URLs in production. Zero mentions of either convention exist in the prompt template.","feedback":"The prompt template should include explicit instructions: 'Ensure Vite is configured with base=\"/app/\" in vite.config.ts and BrowserRouter is initialized with basename=\"/app\" in the entry point. This ensures assets and routing work correctly when served from the /app/ subpath in production.'"},"design_system":{"passed":false,"quote":"Skip the design system → inconsistent styling","reason":"The AI output confirms that docs/STYLE.md is not referenced anywhere in the prompt template. The output explicitly states 'no `docs/STYLE.md`' and predicts an agent would skip design system compliance, leading to inconsistent styling.","feedback":"The prompt template should add: 'Review docs/STYLE.md for component patterns, color tokens, and spacing conventions before implementing the chart. Ensure the bar chart uses ProvenantAI-standard colors (success/failure), spacing, and typography to maintain visual consistency with the dashboard.'"},"build_command":{"passed":false,"quote":"Run `npx vite build` from the wrong directory","reason":"The AI output notes that the prompt does NOT include the correct build instruction 'cd dashboard-app && npx vite build'. The output predicts agents would run the build from the wrong directory, causing build failures or incorrect output locations.","feedback":"The prompt template should include: 'To build the dashboard for production, run: cd dashboard-app && npx vite build. Verify the dist/ output contains assets with correct /app/ prefixes before deployment.'"},"product_gating":{"passed":false,"quote":"Add a COO-visible chart without checking whether the feature should be gated to COO-tier only","reason":"The AI output identifies that while the prompt mentions 'COO dashboard' as the destination, it provides NO guidance on product-gating logic or access control. The output correctly notes that an agent could add this feature without implementing tier-based visibility checks, exposing it to unauthorized user tiers.","feedback":"The prompt template should add: 'This chart is visible only to COO-tier users. Implement access control by wrapping the component with a product-gating check (e.g., hasAccessToFeature(\"recipe_execution_history\", userTier)). Verify the chart is hidden for CMO/CFO users in tests.'"}},"judgeMode":"llm","duration":35493,"output":"The project only contains `CLAUDE.md` — no `dashboard-app/`, no `docs/STYLE.md`. Evaluating the prompt template against the four ProvenantAI dashboard conventions:\n\n| Convention | Present in prompt? |\n|---|---|\n| Vite `base='/app/'` + `BrowserRouter` basename | **No** |\n| Design system reference (`docs/STYLE.md`) | **No** |\n| Build via `cd dashboard-app && npx vite build` | **No** |\n| Product-gating mention (CMO/CFO/COO) | **No** (mentions \"COO dashboard\" as destination, but says nothing about gating logic) |\n\n**All four are missing.** The prompt is a generic issue-to-instructions template — it passes through the Linear description verbatim but adds no project-specific guardrails. An agent following it would likely:\n- Build without the `base='/app/'` path prefix → broken asset URLs in production\n- Skip the design system → inconsistent styling\n- Run `npx vite build` from the wrong directory\n- Add a COO-visible chart without checking whether the feature should be gated to COO-tier only\n"}